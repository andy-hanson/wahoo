package org.wahoo
package gameObject
package graphic

import shape.Vec

/*
class Billboard(
	pos: Vec,
	name: String,
	frames: Int,
	speed: Int,
	loop: Boolean = true,
	scale: Int = 1)
	extends StillMoo(pos)
{
	override def animationDirectory =
		name

	def AniIdle =
		ani("idle", frames, speed, loop, scale)
}
*/